<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Display Data</title>
</head>
<body>
    <h1>Data List</h1>
    <ul>
        <% users.forEach(user => { %>
            <li>
                <strong>BranchName:</strong> <%= user.BranchName %>, 
                <strong>Dependency:</strong> <%= user.Dependency %>, 
                <strong>ChangeLog:</strong> <%= user.ChangeLog %>, 
                <strong>TicketID:</strong> <%= user.TicketID %>, 
                <strong>DeveloperName:</strong> <%= user.DeveloperName %>
            </li>
        <% }) %>
    </ul>
</body>
</html>